"use client";
/** @type {import('next').NextConfig} */
const nextConfig = {images: {
                                domains: ['localhost', 'www.bing.com']

                            } };

export default nextConfig;
